{
  "name": "ubi-container",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix web_dashboard",
    "dev": "npm run dev --prefix web_dashboard",
    "start": "npm run start --prefix web_dashboard"
  }
}
